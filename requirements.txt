# Cognitect dependencies
# Core AI / NLP
anthropic>=0.25.0

# Constraint solver
kiwisolver>=1.4.5

# 2D CAD export
ezdxf>=1.1.0

# Plan preview rendering (PNG/PDF)
matplotlib>=3.8.0
Pillow>=10.0.0

# Parametric CAD (auxiliary)
cadquery>=2.4.0

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # required for FastAPI file uploads (multipart/form-data)

# Task queue
celery[redis]>=5.3.0
redis>=5.0.0

# Database
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# Persistence (Supabase) + auth
supabase>=2.0.0
# [crypto] extra pulls in cryptography for ES256/RS256 verification of
# Supabase's asymmetric JWTs (post ~mid-2026 default). Legacy HS256 still works.
PyJWT[crypto]>=2.8.0

# Validation / config
pydantic>=2.0.0
python-dotenv>=1.0.0

# Error tracking
sentry-sdk[fastapi]>=2.0

# Testing
# httpx pinned <0.28: 0.28 dropped TestClient's `app=` shortcut, which the
# pinned starlette (via fastapi==0.104.1) still relies on. `supabase` package
# pulls in latest httpx by default — pin here to avoid the conflict.
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0,<0.28
