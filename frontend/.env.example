NEXT_PUBLIC_SUPABASE_URL=https://ynyjxmgfkptlvxrnfgmq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<paste from SECRETS.md>
NEXT_PUBLIC_API_URL=http://localhost:8000
# Must point to a backend that serves the Architecture C endpoint
# POST /v2/plans/generate (used by the /try spec builder). Production is
# Architecture C-era already: NEXT_PUBLIC_API_URL=https://cognitect-api-247099587583.us-central1.run.app

# Sentry error tracking (frontend)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=  # for source map upload during build
