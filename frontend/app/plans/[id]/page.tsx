export default function PlanPage({ params }: { params: { id: string } }) {
  // TODO(task-6): SSR-load the plan and render <PlanEditor planId={params.id} anonymous={false} />.
  return <div>TODO {params.id}</div>;
}
